# grosiree.github.io

list for grocery

## Code Runner

A simple page `run-code.html` lets you experiment with JavaScript directly in the browser. Open the file in your browser, type some JavaScript code, and click **Run Code** to see the result below the text area.
